// Simple wrapper for electron-store in future
export const store = {
  async get(key) {
    // placeholder: connect to preload if storing via main
    return null;
  },
  async set(key, val) {
    return null;
  }
};
